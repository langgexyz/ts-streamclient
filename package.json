{
  "_ai_rules": {
    "github_dependencies": {
      "langgexyz/*": {
        "version_pattern": "semver:\\^[1-9][0-9]*\\.[0-9]+\\.[0-9]+$",
        "forbidden_patterns": [
          "semver:\\^[0-9]+\\.[0-9]+\\.[0-9]+-[a-zA-Z]",
          "semver:\\^[0-9]+\\.[0-9]+\\.[0-9]+\\.[0-9]+"
        ],
        "description": "package.json 中只能使用稳定版本范围，如 ^1.0.0，禁止直接写预发布版本如 ^1.0.0-alpha.4"
      }
    }
  },
  "name": "ts-streamclient",
  "version": "1.0.2",
  "description": "typescript stream client",
  "author": "xpwu",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "browser": "dist/umd/index.js",
  "dependencies": {
    "ts-concurrency": "github:langgexyz/ts-concurrency#91598d9",
    "ts-xutils": "github:langgexyz/ts-xutils#semver:^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "28.1.3",
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.12",
    "class-transformer": "^0.5.1",
    "jest": "28.1.1",
    "ts-jest": "28.0.5",
    "typescript": "^5.0.0",
    "ws": "^8.18.0"
  },
  "scripts": {
    "build": "npm run build:cjs && npm run build:esm && npm run build:umd && npm run build:types",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "build:esm": "tsc -p tsconfig.json",
    "build:umd": "tsc -p tsconfig.umd.json",
    "build:types": "tsc -p tsconfig.types.json",
    "tsc": "tsc",
    "test": "jest"
  },
  "keywords": [
    "ts",
    "typescript",
    "stream client",
    "web"
  ],
  "license": "MIT",
  "files": [
    "dist",
    "src",
    "index.ts"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/langgexyz/ts-streamclient.git"
  },
  "jest": {
    "preset": "ts-jest",
    "testRegex": "test/(.+)\\.test\\.(tsx?)$",
    "transformIgnorePatterns": [
      "/node_modules/(?!(ts-xutils|ts-concurrency)/)"
    ]
  },
  "miniprogram": "."
}
